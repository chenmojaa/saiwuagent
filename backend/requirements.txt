# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Config & validation
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0

# Agent core (LangChain + LangGraph)
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langchain-anthropic>=1.0
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0
# graph.py 直接 import aiosqlite（AsyncSqliteSaver 的连接）
aiosqlite>=0.19

# HTTP / streaming
httpx>=0.27
python-multipart>=0.0.20
sse-starlette>=2.0

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

# Storage / RAG
chromadb>=0.5
sqlmodel>=0.0.22
# api/notes.py 直接使用 SQLAlchemy 的 text()/Engine
sqlalchemy>=2.0
trafilatura>=1.12
pypdf>=5.0
pdfplumber>=0.11

# Document parsers
python-docx>=1.0
python-pptx>=1.0
openpyxl>=3.1
Pillow>=10.0
pytesseract>=0.3
beautifulsoup4>=4.12

# ---- 可选：CrossEncoder 重排 ----
# agent/rerank.py 用 sentence-transformers 做二阶段精排（agent/nodes/retrieve.py
# 会在重排可用时放宽召回深度到 50）。不装也能跑：retrieve 会自适应降级为
# 窄召回（RECALL_PLAIN=8），只是没有精排效果。
# 注意体积较大（含 torch，首次还需下载约 2GB 模型），按需安装：
#   pip install sentence-transformers>=3.0
# sentence-transformers>=3.0
